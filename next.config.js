/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Tambahkan konfigurasi jika Anda menggunakan image optimization dari external service
}

module.exports = nextConfig